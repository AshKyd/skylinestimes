{% from "partials/teaser.njk" import teaser with context %}
<aside>
  <h2>Just In</h2>
  <ul class="teaserlist teaserlist--vertical">
  {% for post in collections.posts.slice(0, 5) %}
      <li class="teaserlist__item">
        {{ teaser(post, size="small", meta=false, text=false) }}
      </li>
  {% endfor %}
  </ul>
</aside>