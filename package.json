{
  "name": "skylinestimes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "ASSET_ROOT=/assets eleventy && parcel build src/theme/index.js -d _site/assets && cp -r src/wp-admin _site/",
    "start": "npm run build && eleventy",
    "start-server": "ASSET_ROOT=http://localhost:1234 eleventy --serve",
    "start-assets": "parcel src/theme/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "@11ty/eleventy-plugin-rss": "^1.0.7",
    "sass": "^1.26.5",
    "moment": "^2.24.0",
    "parcel": "^1.12.4"
  },
  "dependencies": {}
}
